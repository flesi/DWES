<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php
        // Condicional Simple ( if )
        if (condition) {
            # code...
        }
        
        // Condicional Doble ( if-else )
        if (condition) {
            # code...
        } else {
            # code...
        }

        // Operador Ternario

        // Codicional multiple (if-elseif-else)
        if (condition) {
            # code...
        } else if (condition) {
            # code...
        } else {
            # code...
        }
        
        //Condicional anidadas
        //Selección multiple(switch)
        switch ($variable) {
            case 'value':
                # code...
                break;
            
            default:
                # code...
                break;
        }

        //Selección multiple(match)
        match (expression) {
             => ,
             => ,
        }


    ?>
</body>
</html>